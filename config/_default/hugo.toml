# -- Site Configuration --
# Refer to the theme docs for more details about each of these parameters.
# https://jpanther.github.io/congo/docs/getting-started/

baseURL = "https://bravebuffalo.cc/"
theme = "congo"
defaultContentLanguage = "zh-CN"

enableRobotsTXT = true
summaryLength = 50

[pagination]
  pagerSize = 10
  
[outputs]
  home = ["HTML", "RSS", "JSON"]
